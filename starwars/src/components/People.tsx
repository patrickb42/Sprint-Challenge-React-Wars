import React, { useState, useEffect} from 'react';


interface PeopleProps {
  response: any;
}
const People = ({ response }: PeopleProps) => {
  // console.log(`people response ${response}`);
  // const personResponses1: any = response.then(personResponses => {
  //   return personResponses.map(personResponse => personResponse)
  // });
  // console.log(`person reponses ${personResponses1.then(a => a)}`);
  
  return (
    <>
    </>
  );
};

export default People;
